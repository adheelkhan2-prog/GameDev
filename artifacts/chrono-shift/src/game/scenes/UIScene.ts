import Phaser from "phaser";

// Placeholder UI scene (HUD is managed per-level via UIManager)
export class UIScene extends Phaser.Scene {
  constructor() {
    super("UIScene");
  }

  create() {}
}
